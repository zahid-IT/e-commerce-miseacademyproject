Thank you for installing {{ .Chart.Name }}.

Your release is named {{ .Release.Name }}.

To get the backend URL, run these commands:
{{- if .Values.ingress.enabled }}
  https://{{ index .Values.ingress.hosts 0 host }}
{{- else if contains "NodePort" .Values.service.type }}
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "backend.fullname" . }})
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo http://$NODE_IP:$NODE_PORT
{{- else }}
  kubectl port-forward --namespace {{ .Release.Namespace }} svc/{{ include "backend.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}
  echo "Visit http://127.0.0.1:{{ .Values.service.port }} to use your application"
{{- end }}

To check the status: kubectl get pods -n {{ .Release.Namespace }}

To run tests: helm test {{ .Release.Name }} -n {{ .Release.Namespace }}

To upgrade: helm upgrade {{ .Release.Name }} . -f values.yaml

To rollback: helm rollback {{ .Release.Name }} -n {{ .Release.Namespace }}

MongoDB connection details:
  Username: {{ .Values.mongodb.auth.username }}
  Database: {{ .Values.mongodb.auth.database }}
